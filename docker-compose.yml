services:
  brain:
    build: .
    image: brain:latest
    container_name: brain
    restart: unless-stopped
    ports:
      - "127.0.0.1:8765:8765"
      - "127.0.0.1:8766:8766"
    volumes:
      - ./data:/app/data
